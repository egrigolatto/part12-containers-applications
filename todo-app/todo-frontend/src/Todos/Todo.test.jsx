// import { render, screen } from "@testing-library/react";
// import { Todo } from "./Todo";

test("renders todo", () => {
  // render(<Todo todo={"test"} />);
  // const linkElement = screen.getByText(/test/i);
  // expect(linkElement).toBeInTheDocument();
});
